import styled from 'styled-components';

const Wrapper = styled.div`
  width: 40px;
  height: 40px;
  position: absolute;
  top: calc(50% - 40px);
  left: calc(50% - 40px);
`;

export default Wrapper;
